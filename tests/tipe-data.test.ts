describe('Data type', function (){
    it('should must declare', function (){
        let name : String = "fuji";
        let price : Number = 100000;
        let isAdmin : Boolean = true;

        console.info(name)
        console.info(price)
        console.info(isAdmin)

        // name = 100;
        // price  = "fuji";
        // isAdmin = 1;
    })
})